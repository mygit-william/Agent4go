version: "3.8"

services:
  nanobot:
    build: .
    container_name: nanobot
    volumes:
      - ./config:/app/config
      - ./storage:/app/storage
      - ./memory:/app/memory
    restart: unless-stopped
    stdin_open: true
    tty: true
